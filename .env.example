# Required to actually run the agent (npm start). Not needed for npm run smoke-test.
# Skip this if you're already logged in via `claude setup-token`.
ANTHROPIC_API_KEY=

# Comma-separated domains/lanes research considers each cycle. Proposals can
# come from whichever domain looks best that cycle -- they aren't forced to
# rotate evenly. Fewer, narrower lanes still make the lesson store more
# useful faster (outcomes per domain accumulate quicker); this is a
# deliberate tradeoff for broader coverage.
AGENT_DOMAINS=micro-SaaS tool for developers (self-built and self-hosted),Chrome extension for developers,VS Code extension for developers

# How often (ms) the main loop starts a new research+plan cycle. Default: 1h.
AGENT_CYCLE_INTERVAL_MS=3600000

# Research pauses (doesn't stop, just skips proposing more) once this many
# proposals are sitting pending review, so the queue can't grow unbounded
# while you're away. Default: 5
AGENT_MAX_PENDING_PROPOSALS=5

# Path to the SQLite database file. Default: ./data/agent.db
AGENT_DB_PATH=./data/agent.db

# Port for the web UI's REST + WebSocket API (src/server.ts). Default: 4001
AGENT_SERVER_PORT=4001

# ---- integrations (act-phase tools; each optional -- omit to leave that
# integration's tools unavailable rather than fail at startup) -------------

# GitHub personal access token, repo scope. Enables reading repos and,
# once a proposal requesting it is approved, creating repos/branches/PRs.
GITHUB_TOKEN=

# Vercel API token. Enables reading projects and, once approved, deploying.
VERCEL_TOKEN=

# Netlify API token. Enables reading sites and, once approved, deploying.
NETLIFY_AUTH_TOKEN=

# Voyage AI API key (Anthropic's recommended embeddings provider) for
# semantic search over research notes and lessons. Without this set,
# search silently falls back to the old LIKE-based text match.
VOYAGE_API_KEY=
